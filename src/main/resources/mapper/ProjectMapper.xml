<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.portfolio.project.mapper.ProjectMapper">

    <insert id="save" parameterType="com.portfolio.project.domain.Project" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO projects (
            title,
            summary,
            description,
            thumbnail_url,
            feature_image_urls,
            feature_image_captions,
            project_url,
            start_date,
            end_date,
            use_yn,
            tech_stack,
            my_role,
            trouble_shooting,
            github_url,
            deploy_url,
            result,
            created_at,
            updated_at
        )
        VALUES (
            #{title},
            #{summary},
            #{description},
            #{thumbnailUrl},
            #{featureImageUrls},
            #{featureImageCaptions},
            #{projectUrl},
            #{startDate},
            #{endDate},
            #{useYn},
            #{techStack},
            #{myRole},
            #{troubleShooting},
            #{githubUrl},
            #{deployUrl},
            #{result},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="findById" parameterType="long" resultType="com.portfolio.project.domain.Project">
        SELECT
            id,
            title,
            summary,
            description,
            thumbnail_url,
            feature_image_urls,
            feature_image_captions,
            project_url,
            start_date,
            end_date,
            use_yn,
            tech_stack,
            my_role,
            trouble_shooting,
            github_url,
            deploy_url,
            result,
            created_at,
            updated_at
        FROM projects
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.portfolio.project.domain.Project">
        SELECT
            id,
            title,
            summary,
            description,
            thumbnail_url,
            feature_image_urls,
            feature_image_captions,
            project_url,
            start_date,
            end_date,
            use_yn,
            tech_stack,
            my_role,
            trouble_shooting,
            github_url,
            deploy_url,
            result,
            created_at,
            updated_at
        FROM projects
        ORDER BY id DESC
    </select>

    <update id="update" parameterType="com.portfolio.project.domain.Project">
        UPDATE projects
        SET
            title = #{title},
            summary = #{summary},
            description = #{description},
            thumbnail_url = #{thumbnailUrl},
            feature_image_urls = #{featureImageUrls},
            feature_image_captions = #{featureImageCaptions},
            project_url = #{projectUrl},
            start_date = #{startDate},
            end_date = #{endDate},
            use_yn = #{useYn},
            tech_stack = #{techStack},
            my_role = #{myRole},
            trouble_shooting = #{troubleShooting},
            github_url = #{githubUrl},
            deploy_url = #{deployUrl},
            result = #{result},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM projects
        WHERE id = #{id}
    </delete>

</mapper>
